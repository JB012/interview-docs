services:
  server:
    build: ./server
    ports:
      - "8080:8080"
    env_file:
      - ./server/.env
    environment:
      -  MICRONAUT_ENVIRONMENTS=dev
  client:
    build: ./client
    ports:
    - "4200:4200"
    env_file:
      - ./client/.env
    depends_on:
      - server